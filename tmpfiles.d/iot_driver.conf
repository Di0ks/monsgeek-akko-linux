# Type Path                Mode User Group Age Argument
# Pre-create the depth-server runtime dir with the access group so non-root
# members can traverse it; the socket itself is chown'd by iot_driver via
# IOT_DRIVER_DEPTH_GROUP (see iot_driver depth-server).
d /run/iot_driver 0755 root input - -
